[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"

[functions]
  node_bundler = "esbuild"

# Deploy serverless functions to Singapore (closest to Neon DB)
[functions."*"]
  region = "ap-southeast-1"

[[plugins]]
  package = "@netlify/plugin-nextjs"
